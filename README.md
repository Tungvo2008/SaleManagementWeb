Repo gồm:

- `backend/` — FastAPI
- `frontend/` — React (CRA)

## Run Backend (FastAPI)

```bash
cd backend
source .venv/bin/activate
python3 -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```
